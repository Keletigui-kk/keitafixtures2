{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <div class="container">
        {% if app.user %}
        <div class="row" id="marow">
            <div class="col-md-3" id="col1">
                {% for post in post %}
                <div class="card">
                    <img class="card-img-top" src="{{asset('images/' ~ post.image)}}"  class="responsive-img" alt="Card image cap"> <!--on peut avoir un lien d'image sur le site placeholder.com pour avoir des images randon pour les fausses infos-->
                    <div class="card-body">
                        <h5 class="card-title">{{post.title}}</h5>
                        <p class="card-text">{{post.content}}</p>
                        <p>{{post.author}} - {{post.createdAt |date('d/m/y')}}</p>
                         <a href="{{path('show_post',{id:post.id})}}" class="btn btn-primary">voir l'article</a>
                    </div>
                </div>
                {% endfor %}
            </div>
               
               
            <div class="col-md-6" id="col2">
               {% for post in post1 %}
                <div class="card">
                    <img class="card-img-top" src="{{asset('images/' ~ post.image)}}"  class="responsive-img" alt="Card image cap"><!--on peut avoir un lien d'image sur le site placeholder.com pour avoir des images randon pour les fausses infos-->
                    <div class="card-body">
                        <h5 class="card-title">{{post.title}}</h5>
                        <p class="card-text">{{post.content}}</p>
                        <p>{{post.author}} - {{post.createdAt |date('d/m/y')}}</p>
                        <a href="{{path('show_post',{id:post.id})}}" class="btn btn-primary">voir l'article</a>
                    </div>
                </div>
                {% endfor %}
            </div>
               
            <div class="col-md-3" id="col3">
                {% for post in post2 %}
                <div class="card">
                    <img class="card-img-top" src="{{asset('images/' ~ post.image)}}"  class="responsive-img" alt="Card image cap"><!--on peut avoir un lien d'image sur le site placeholder.com pour avoir des images randon pour les fausses infos-->
                    <div class="card-body">
                        <h5 class="card-title">{{post.title}}</h5>
                        <p class="card-text">{{post.content}}</p>
                        <p>{{post.author}} - {{post.createdAt |date('d/m/y')}}</p>
                        <a href="{{path('show_post',{id:post.id})}}" class="btn btn-primary">voir l'article</a>
                    </div>
                </div>
                {% endfor %}
            </div>
              {# {{dump(post.category)}} #}
        </div>
    
        {% else %} 
        <div class="row" id="marow2">
            <div class="col-md-3" id="col1">
                {% for post in post %}
                <div class="card">
                    <img class="card-img-top" src="{{asset('images/' ~ post.image)}}"  class="responsive-img" alt="Card image cap"><!--on peut avoir un lien d'image sur le site placeholder.com pour avoir des images randon pour les fausses infos-->
                    <div class="card-body">
                        <h5 class="card-title">{{post.title}}</h5>
                        <p class="card-text">{{post.content}}</p>
                        <p>{{post.author}} - {{post.createdAt |date('d/m/y')}}</p>
                        <a href="{{ path('app_login')}}" class="btn btn-primary">Voi l'article</a>
                    </div>
                </div>
                {% endfor %}
            </div>
                
                
            <div class="col-md-6" id="col2">
                {% for post in post1 %}
                <div class="card">
                    <img class="card-img-top" src="{{asset('images/' ~ post.image)}}"  class="responsive-img" alt="Card image cap"><!--on peut avoir un lien d'image sur le site placeholder.com pour avoir des images randon pour les fausses infos-->
                    <div class="card-body">
                        <h5 class="card-title">{{post.title}}</h5>
                        <p class="card-text">{{post.content}}</p>
                        <p>{{post.author}} - {{post.createdAt |date('d/m/y')}}</p>
                        <a href="{{ path('app_login')}}" class="btn btn-primary">Voi l'article</a>
                    </div>
                </div>
                {% endfor %}
            </div>
                
                
            <div class="col-md-3" id="col3">
                {% for post in post2 %}
                <div class="card">
                    <img class="card-img-top" src="{{asset('images/' ~ post.image)}}"  class="responsive-img" alt="Card image cap"><!--on peut avoir un lien d'image sur le site placeholder.com pour avoir des images randon pour les fausses infos-->
                    <div class="card-body">
                        <h5 class="card-title">{{post.title}}</h5>
                        <p class="card-text">{{post.content}}</p>
                        <p>{{post.author}} - {{post.createdAt |date('d/m/y')}}</p>
                    <a href="{{ path('app_login')}}" class="btn btn-primary">Voi l'article</a>
                    </div>
                </div>
                {% endfor %}
            </div>
                {% endif %}
        </div>
   </div>
     
{% endblock %}
